
# Архитектурная документация проекта ScreensTest (Unity)

## Общая структура `Assets`

```
Assets/
├── Art/                # Графические ассеты проекта
│   └── Sprites/        # Спрайты для UI и геймплейных объектов
├── Plugins/            # Сторонние библиотеки
│   └── DOTween/        # Tween-анимации (внешний плагин)
├── Prefabs/            # Префабы игровых объектов
│   ├── Controllers/    # Префабы управляющих компонентов (например, MainManager)
│   ├── Sound/          # Префабы звуковых компонентов и аудиосистемы
│   └── UI/             # UI-префабы
│       ├── Buttons/    # Кнопки интерфейса
│       ├── Panels/     # Панели и окна UI
│       ├── Slots/      # Слоты для предметов, навыков и т.п.
├── Resources/          # Ресурсы, доступные в runtime
│   └── Configs/        # ScriptableObject-конфиги, доступные через Resources.Load
├── Scenes/             # Сцены проекта Unity
├── Scripts/            # Скрипты логики
│   ├── General/
│   │   ├── Configs/        # ScriptableObject-настройки, используемые системой
│   │   ├── Controllers/    # Управляющие классы: MainManager, UIManager и т.п.
│   │   ├── DataHolders/    # Статические или сериализуемые классы-хранилища данных
│   │   ├── Interfaces/     # Интерфейсы для модульности и слабой связности
│   │   ├── SoundSystem/    # Реализация аудиосистемы: проигрывание клипов, фоновой музыки
│   └── UI/
│       ├── Base/           # Базовые классы UI-элементов (например, Panel)
│       ├── Buttons/        # Скрипты для кнопок (логика нажатий и состояний)
│       ├── Panels/         # Поведение и логика UI-панелей
│       └── Slots/          # Компоненты ячеек инвентаря или интерфейса
├── Settings/           # ScriptableObject'ы и настройки Universal Render Pipeline
├── Sound/              # Аудиофайлы проекта
│   ├── Clips/           # Отдельные звуковые эффекты (SFX)
│   └── Musics/          # Фоновые музыкальные треки (BGM)
└── TextMesh Pro/       # TMP ассеты (структура игнорируется)
```

## UI Архитектура (на основе имеющихся данных)

Папка `Prefabs/UI/` содержит компоненты интерфейса:
- **Buttons/** — стандартные кнопки для UI
- **Panels/** — визуальные контейнеры (меню, настройки и т.п.)
- **Slots/** — интерфейсные ячейки (например, инвентарь, навыки)

Также в `Scripts/UI/` используются папки:
- **Base/** — базовые компоненты или абстракции UI
- **Buttons/** — логика UI-кнопок
- **Panels/** — управляющие панели или окна
- **Slots/** — логика для UI-слотов

## Структура UI-слоёв

- **View Layer**: `Prefabs/UI/`, `Scripts/UI/Panels/`
- **Logic Layer**: `Scripts/UI/Buttons/`, `Scripts/UI/Slots/`
- **Base Layer**: `Scripts/UI/Base/`

Архитектура UI построена по принципу модульности с разбиением по ролям компонентов.

## Сцены

- Папка `Scenes/` содержит основной набор игровых или UI-сцен

## Настройки графики

- **UniversalRP.asset** — конфигурация Universal Render Pipeline
- **Renderer2D.asset** — настройки 2D-рендера
- **Lit2DSceneTemplate** — шаблон сцены URP с преднастройками

## Общая логика (General)

- **EntryPoint** — начальная инициализация проекта
- **MainManager** — глобальный синглтон-менеджер
- **Timer** — обработка задержек и таймеров
- **SoundSystem/** — управление звуками
- **Interfaces/** — интерфейсы и контракты
- **DataHolders/** — хранилища состояния
- **Controllers/** — управляющие модули
- **Configs/** — конфигурационные данные

---

## Дальнейшие шаги:

- Составить диаграмму классов
- Описать паттерны (если применяются: Singleton, EventBus, DI и др.)

Документ будет дополняться по мере сбора новых данных.
